#include "main.h"

/**
 * mul - function to multiply
 * @a: first argument of multiplication
 * @b: second argumnet of multi
 * Description: gets 2 integers and multiplies them
 * Return: returns the multiplication value of both integers
 */

int mul(int a, int b)
{
	return (a * b);
}
