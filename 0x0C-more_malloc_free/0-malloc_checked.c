#include "main.h"

/**
 * malloc_checked - function to creates memory allocateion
 * @b: int of alloc
 * Return: return the address of allocated memory
 */

void *malloc_checked(unsigned int b)
{
	int *iptr = malloc(b);

	if (iptr ==)
		exit(98);
	return (iptr);
}
